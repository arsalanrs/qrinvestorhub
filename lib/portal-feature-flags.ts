/** Flip on when investor tier UI (Select Investor / Pro / Elite) is ready for customers. */
export const SHOW_INVESTOR_TIER = false;

/** Flip on when partner links (insurance, etc.) are live in the portal. */
export const SHOW_PORTAL_RESOURCES = false;
